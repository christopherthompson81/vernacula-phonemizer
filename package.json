{
  "name": "vernacula-phonemizer",
  "version": "0.1.0",
  "license": "MIT",
  "files": [
    "src",
    "LICENSES",
    "NOTICE.md",
    "!src/**/*.scratch.*",
    "!src/**/*.log",
    "!src/**/__pycache__"
  ],
  "description": "Canonical-IPA phonemizer — native, rule- and lexicon-based. Hindi, English, Mandarin, Spanish, Arabic.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./browser": "./src/browser.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check:package": "node tools/check-package-fence.mjs",
    "check:goldens": "tsx tools/check-goldens.mts",
    "check:en-gb-sets": "tsx tools/referee-eval/build-en-gb-sets.ts --check",
    "dump:traces": "tsx tools/dump-traces.mts .trace-parity/ts.tsv",
    "check:trace-parity": "npm run dump:traces && dotnet run -c Release --project csharp/tools/trace-parity -- .trace-parity/ts.tsv",
    "ci": "npm run typecheck && npm test && npm run check:goldens && npm run check:package"
  },
  "optionalDependencies": {
    "onnxruntime-node": "^1.27.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^2"
  },
  "engines": {
    "node": ">=22.3"
  },
  "workspaces": [
    "data"
  ],
  "dependencies": {
    "vernacula-phonemizer-data": "0.1.0"
  }
}
