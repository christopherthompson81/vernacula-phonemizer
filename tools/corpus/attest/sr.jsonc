// WIKIPEDIA WORD ATTESTATION — sr (#586). Written by tools/normalization/attest.ts.
//
// A SEPARATE AND WEAKER TIER than the FLEURS corpus, the referees, and espeak's dictsource. Wikipedia is
// user-generated and not audio-aligned, and on a small wiki one article can be a single contributor's
// idiolect — so `articles` is recorded beside `tokenHits`: one hit in one article is a LEAD, not a finding.
//
// `substringOnly` is the column that matters when tokenHits is 0. It is the count of articles where the
// letters appear INSIDE another word, which is exactly how an absent word comes to look sourced —
// lb's `Yen` in `Libyen`, xh's `iiyeni` in `yeNintendo`. A substring-only verdict is a NEGATIVE result.
//
// `bounded` false means the word is in an UNSPACED script (Han, kana, Thai, Khmer, …) where no word boundary
// exists to test, so `tokenHits` is a substring count and the verdict is written `attested*`. The precision
// that makes `Libyen` fail for `Yen` is simply unavailable — a Han hit can always be part of a longer
// compound — so for those the examples are not a courtesy, they are the entire finding.
//
// ⚠ ATTESTATION IS NEVER SUFFICIENT. It proves a word exists, not that it fits the slot. Check the part of
// speech and the sense against the examples before using any of this to justify a reading.
{
    "language": "sr",
    "wiki": "sr.wikipedia.org",
    "tier": "wikipedia (register-restricted)",
    // The article sample was narrowed to this register; the hit test was NOT relaxed. Reproduce with
    // `--context` set to exactly this string.
    "context": "математика аритметика дељење",
    "findings": [
        {
            "word": "једнако",
            "verdict": "attested",
            "bounded": true,
            "tokenHits": 17,
            "articles": 7,
            "substringOnly": 2,
            "examples": [
                "…од четири јабуке, што значи да је двадесет подељено са пет једнако четири. Ово се означава као 20 / 5 = 4, или 20/5 = 4. Оно ш…",
                "…ај остатак додаје количнику као децимала, тако да је 10 / 3 једнако 3+1/3 или 3.33..., али у контексту целобројне поделе, где б…",
                "…рања, дељење није комутативно, што значи да a / b није увек једнако b / a. Дељење, такође, генерално није асоцијативно, што зна…",
                "…о \"0\", а негативна нула као \"-0\". Ове вредности се понашају једнако у нумеричким упоређењима, али неке операције враћају различ…",
                "…м ходу са вредностима где је апсолутна растојање између њих једнако као и за суседне вредности које се налазе изван млазне гран…",
                "…је константно А (440 Hz у обе лествице), горња нота је C♯ у једнако темперованој лествици током прве секунде, и C♯ у лествици ч…"
            ]
        },
        {
            "word": "мање од",
            "verdict": "attested",
            "bounded": true,
            "tokenHits": 3,
            "articles": 1,
            "substringOnly": 0,
            "examples": [
                "…њеним до првог мањег производа, 700:87*7, јер је 87*7=609 - мање од 700, а 88*8=704 - превише. Поново одузимамо и спуштамо след…",
                "…воструким резултатом. Сада је допуна 5, тј. 9585*5=47925 је мање од 55900, док је 9586*6=57516 преступ (више од 55900) итд. При…",
                "…езултатом 36 коме допишемо 4 тако да је 364*4=1456 још увек мање од 1750. Опет одузимамо 1750-1456=294, спуштамо нову класу, са…"
            ]
        },
        {
            "word": "веће од",
            "verdict": "absent",
            "bounded": true,
            "tokenHits": 0,
            "articles": 0,
            "substringOnly": 0,
            "examples": []
        },
        {
            "word": "подељено са",
            "verdict": "attested",
            "bounded": true,
            "tokenHits": 2,
            "articles": 2,
            "substringOnly": 0,
            "examples": [
                "…дели у пет група од четири јабуке, што значи да је двадесет подељено са пет једнако четири. Ово се означава као 20 / 5 = 4, или 20/…",
                "…\" – 38 је конгруентно са 14 по модулу 12, али остатак од 14 подељено са 12 је 2, не 38. Да би се избегли неспоразуми, релација конг…"
            ]
        }
    ],
}
